id: ErTools_BitErrorRate
label: BitErrorRate
category: '[ErTools]'

templates:
  imports: from gnuradio import ErTools
  make: ErTools.BitErrorRate(${N})

parameters:
- id: N
  label: Pocet bodov BER
  dtype: int
  default: 8

inputs:
 - label: in1
   domain: stream
   dtype: byte
 - label: in2
   domain: stream
   dtype: byte

outputs:
 - label: out1
   domain: stream
   dtype: float

file_format: 1
