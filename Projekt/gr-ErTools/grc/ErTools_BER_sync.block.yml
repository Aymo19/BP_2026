id: ErTools_BER_sync
label: BER_sync
category: '[ErTools]'

templates:
  imports: from gnuradio import ErTools
  make: ErTools.BER_sync(${N})

parameters:
- id: N
  label: N
  dtype: int
  default: 128

inputs:
 - label: in0
   domain: stream
   dtype: int

 - label: in1
   domain: stream
   dtype: int
  
 - label: in2
   domain: stream
   dtype: int

outputs:
 - label: out0
   domain: stream
   dtype: float

file_format: 1
