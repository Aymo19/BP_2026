id: ErTools_BER
label: BER
category: '[ErTools]'

templates:
  imports: from gnuradio import ErTools
  make: ErTools.BER(${N})

parameters:
- id: N
  label: Pocet SNR bodov
  dtype: int
  default: 128

inputs:
 - label: in0
   domain: stream
   dtype: int
   optional: true
 - label: in1
   domain: stream
   dtype: byte
 - label: in2
   domain: stream
   dtype: byte

outputs:
 - label: out
   domain: stream
   dtype: float
   #vlen

file_format: 1
