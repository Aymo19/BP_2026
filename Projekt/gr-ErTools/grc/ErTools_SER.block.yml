id: ErTools_SER
label: SER
category: '[ErTools]'

templates:
  imports: from gnuradio import ErTools
  make: ErTools.SER(${N})

parameters:
- id: N
  label: Počet Es/N0 bodov
  dtype: int
  default: 128

inputs:
 - label: in0
   domain: stream
   dtype: int

 - label: in1
   domain: stream
   dtype: byte

 - label: in2
   domain: stream
   dtype: byte

outputs:
 - label: out
   domain: stream 
   dtype: float

file_format: 1
